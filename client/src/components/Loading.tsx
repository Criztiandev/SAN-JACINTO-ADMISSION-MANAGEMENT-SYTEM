const Loading = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full bg-black z-50 bg-cover border flex justify-center items-center">
      Loading
    </div>
  );
};

export default Loading;
