import Sidebar from "../../components/Sidebar";

const Dashboard = () => {
  return (
    <section className="grid grid-cols-dashboard-layout h-[100vh]">
      <Sidebar />
    </section>
  );
};

export default Dashboard;
